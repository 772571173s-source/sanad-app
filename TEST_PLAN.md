# خطة الاختبار — Sanad App

## مبدأ الاختبار

- كل Flow يُختبر على **Android** أولاً (المنصة الأساسية)
- ثم يُختبر على **Windows** بعد التأكد من Android
- التحقق من البيانات: عبر **شاشة التطبيق** أولاً (قابلية الاستخدام)، ثم عبر **قراءة قاعدة البيانات** إذا لزم الأمر
- لا حاجة لإنشاء بيانات وهمية قبل كل اختبار (نبدأ من قاعدة نظيفة)

---

## Flow 1: إدارة المركز والحسابات

### الخطوات:
1. فتح التطبيق لأول مرة ← يجب رؤية شاشة الإعداد الأولي
2. إنشاء حساب المالك (مالك سند)
3. تسجيل الدخول بحساب المالك
4. إنشاء مركز جديد: اسم، عنوان، هاتف
5. إنشاء مدير مركز: بريد إلكتروني، اسم، كلمة مرور
6. تسجيل الخروج
7. تسجيل الدخول بحساب مدير المركز
8. إنشاء أخصائي + منسق + مدخل بيانات

### النتائج المتوقعة:
- شاشة الإعداد الأولي تظهر فقط في أول تشغيل (أو بعد مسح البيانات)
- Dashboard المالك يظهر جميع المراكز
- Dashboard المدير يظهر مركزه فقط
- الموظفون يظهرون في شاشة الموظفين

### التحقق من البيانات:
- centers: جميع الأعمدة صحيحة
- users: role صحيح لكل مستخدم

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 2: إضافة طالب وربطه بأخصائي

### الخطوات:
1. تسجيل الدخول كمدخل بيانات (أو مدير)
2. إضافة طالب: اسم، عمر، تشخيص، حالة، برنامج
3. تسجيل الخروج
4. تسجيل الدخول كمنسق
5. توزيع الطالب على أخصائي
6. تسجيل الخروج
7. تسجيل الدخول كأخصائي
8. التحقق من رؤية الطالب في القائمة

### النتائج المتوقعة:
- الطالب يظهر للأخصائي بعد التوزيع
- الطالب لا يظهر للأخصائيين الآخرين
- Dashboard الأخصائي يظهر الطلاب الموزعين فقط
- Dashboard المنسق يظهر كل الطلاب

### التحقق من البيانات:
- students: العمود صحيح
- student_specialists: specialistId صحيح

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 3: إضافة برنامج علاجي

### الخطوات:
1. تسجيل الدخول كمالك سند
2. بناء البرنامج العلاجي:
   - اسم البرنامج، وصف
   - `usesSpeechSounds = false` (برنامج عادي)
3. إضافة أقسام التقييم:
   - قسم 1: مهارات تواصل
   - قسم 2: تفاعل اجتماعي
4. لكل قسم، إضافة عناصر تقييم:
   - عنصر 1: سؤال (singleChoice)
   - عنصر 2: سؤال آخر (multiSelect)
5. لكل عنصر، إضافة خيارات:
   - خيار عادي: `generatesTherapy = false`
   - خيار علاجي: `generatesTherapy = true` مع goalTemplate, therapyTemplate, weaknessTemplate
6. إضافة خطوات مهارات للخيارات العلاجية:
   - خطوة 1: "تقليد الحركات"
   - خطوة 2: "تنفيذ التعليمات"
7. تكرار مع برنامج `usesSpeechSounds = true`:
   - إضافة speechSoundTrigger: حرف "س"، خطأ "إبدال"، موضع "بداية"
   - مع skill_steps_json: ["نطق الحرف منفردًا", "نطق الحرف في مقطع"]

### النتائج المتوقعة:
- البرنامج يظهر في شاشة بناء الهيكل العلاجي
- الأقسام والعناصر والخيارات مترابطة بشكل صحيح
- speechSoundTrigger مرتبط بالبرنامج
- خطوات المهارات مرتبطة بالخيار أو الصوت

### التحقق من البيانات:
- therapy_program_templates: صحيح
- assessment_section_templates: programId صحيح
- assessment_item_templates: sectionId صحيح
- assessment_option_templates: itemId صحيح
- skill_step_templates: ownerId = option.id / trigger.id
- speech_sound_trigger_templates: programId صحيح, skill_steps_json غير فارغة

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 4: تقييم علاجي كامل ⭐

### الإعداد:
- لدينا طالب (من Flow 2) مرّ عليه 6 أشهر في التشخيص
- لدينا برنامج (من Flow 3) مع أقسام وعناصر
- الطالب موزع على الأخصائي

### الخطوات:
1. تسجيل الدخول كأخصائي
2. اختيار الطالب ← التقييم العلاجي
3. اختيار البرنامج العلاجي
4. **لبرنامج عادي (usesSpeechSounds = false)**:
   - قسم 1: اختر خيارًا علاجيًا (generatesTherapy = true)
   - قسم 2: اختر خيارًا عاديًا (generatesTherapy = false)
   - تابع حتى النهاية ← استعراض النتائج
   - احفظ التقييم
5. **لبرنامج أصوات (usesSpeechSounds = true)**:
   - قيّم حرف "س" كخاطئ (إبدال، بداية)
   - قيّم حرف "ر" كصحيح
   - احفظ التقييم

### النتائج المتوقعة:
- **بعد الحفظ**:
  - تظهر رسالة نجاح
  - يختفي شاشة التقييم ← يعود للوحة الأخصائي
  - عند فتح ملف الطالب: الأهداف النشطة تظهر الخطة الجديدة
- **للخطة من التقييم العادي**:
  - sourceType = 'standard'
  - GoalSkillSteps موجودة (من skill_step_templates)
- **للخطة من تقييم الأصوات**:
  - sourceType = 'speechSound'
  - GoalSkillSteps موجودة (من skill_steps_json)

### التحقق من البيانات:
- clinical_assessments: سجل تقييم
- clinical_findings: نتائج لكل عنصر/صوت
- training_plans: للنتائج غير الطبيعية مع goal غير فارغ
- goal_skill_steps: لكل finding مع goal
- assessment_drafts: محذوف بعد الحفظ

### تحقق خاص:
- programId على clinical_finding == id البرنامج المختار
- sourceType على clinical_finding == 'standard' أو 'speechSound'
- نفس القيم منتقلة إلى training_plans و goal_skill_steps

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 5: جلسة علاجية ⭐

### الإعداد:
- لدينا طالب مع خطة علاجية (من Flow 4)
- الخطة تحتوي على GoalSkillSteps

### الخطوات (برنامج عادي):
1. الأخصائي → الجلسات
2. اختر الطالب (إذا لم يكن مختارًا)
3. اختر البرنامج العلاجي
4. إذا البرنامج usesSpeechSounds == true: اختر "أقسام التقييم" (standard)
5. يجب رؤية الهدف والخطوة الأولى
6. قيّم الخطوة بـ **"متقن"**
7. يجب الانتقال إلى الخطوة التالية تلقائيًا
8. قيّم الخطوة الثانية بـ **"بمساعدة"**
9. يجب الانتقال إلى الخطوة التالية
10. قيّم بقية الخطوات
11. يجب رؤية "انتهت عناصر هذه الجلسة"
12. اضغط "اختيار مسار آخر"

### النتائج المتوقعة:
- الخطوات تظهر واحدة تلو الأخرى
- لا توجد حلقة لا نهائية
- بعد كل تقييم، التالي يظهر فورًا
- في النهاية، رسالة الانتهاء تظهر
- "اختيار مسار آخر" يعيد اختيار المصدر (مع بقاء البرنامج)

### التحقق من البيانات:
- sessions: جلسة واحدة لكل تقييم خطوة
- goal_skill_steps: status محدث لكل خطوة
- student_followups:
  - لا يوجد followup لـ 'متقن' (أو إذا كان موجودًا، يكون مقفولاً)
  - يوجد followup (pending) لـ 'بمساعدة' (reason = 'assisted')
  - لا يوجد after session للمرة الأولى لـ 'يحتاج إعادة'

### اختبار خاص:
- افتح الجلسات مرة أخرى ← يجب رؤية "انتهت عناصر هذه الجلسة" (بما أن لا خطوات 'لم يبدأ' متبقية)
- اضغط "اختيار مسار آخر" ← اختر مصدر آخر (speechSound إذا متاح)
- الخطوات المقيّمة في الجلسة الأولى يجب ألا تظهر مرة أخرى (حتى لو status != متقن)

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 6: بمساعدة / يحتاج إعادة / متقن ⭐

### الإعداد:
- أتممنا Flow 5 مع تقييمات مختلفة

### الخطوات:
1. افتح ملف الطالب ← الأهداف النشطة
2. **ابحث عن الخطوات المقيمة بـ 'بمساعدة'**:
   - يجب رؤية أيقونة برتقالية
   - يجب رؤية زر "إعادة التدريب"
3. **ابحث عن الخطوات المقيمة بـ 'متقن'**:
   - يجب رؤية أيقونة خضراء
   - لا يوجد زر "إعادة التدريب"
4. **ابحث عن الخطوات غير المقيمة (لم يبدأ)**:
   - يجب رؤية أيقونة رمادية
   - لا يوجد زر "إعادة التدريب"
   - إذا ظهر في ملف الطالب بهذه الحالة، يجب ألا تظهر في "الأهداف النشطة" بدلاً من "الأهداف المنجزة" (لأن progress < 100)

### تحقق خاص:
- افتح قسم "يحتاج إعادة تدريب" ← يجب رؤية نفس الخطوات
- افتح شاشة الجلسات ← لا تظهر الخطوات (لأنها إما 'بمساعدة' أو 'يحتاج إعادة')

### إعادة التدريب (من ملف الطالب):
1. اضغط "إعادة التدريب" على خطوة 'بمساعدة'
2. يجب فتح شاشة الجلسات مع نفس البرنامج والمصدر
3. الخطوة يجب أن تظهر (الحالة: 'بمساعدة')
4. قيّم بـ 'متقن'
5. ارجع لملف الطالب ← الخطوة الآن 'متقن' ← لا يوجد زر إعادة تدريب
6. followup مقفول (resolvedAt غير فارغ)

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 7: إعادة التدريب من ملف الطالب

### الإعداد:
- طالب مع خطة وخطوات بعضها 'بمساعدة' أو 'يحتاج إعادة'

### الخطوات:
1. ملف الطالب ← قسم "يحتاج إعادة تدريب"
2. اضغط على إحدى المتابعات
3. يجب فتح شاشة الجلسات مع الـ preselect صحيح
4. الخطوة تظهر مع اسمها وحالتها
5. قيّم بـ "متقن" ← followup يُغلق
6. ارجع لملف الطالب ← قسم "يحتاج إعادة تدريب" اختفى

### النتائج المتوقعة:
- sessionPreselect ينقل المستخدم إلى الجلسات بشكل صحيح
- _applyPreselect يختار الطالب، البرنامج، المصدر، الهدف، والخطوة
- بعد التقييم بـ 'متقن': followup = completed
- بعد التقييم بـ 'بمساعدة': followup يبقى pending (upsert)

### التحقق من البيانات:
- student_followups: status, resolvedAt صحيحين
- goal_skill_steps: status محدث

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 8: الواجبات وولي الأمر

### الخطوات:
1. أخصائي → الواجبات
2. اختر طالب
3. اختر نوع: "تمرين خطوة" ← اختر الهدف ← اختر الخطوة
4. املأ النموذج: العنوان + التعليمات + تاريخ الاستحقاق
5. احفظ
6. اذهب لملف الطالب ← قسم الواجبات ← يجب رؤية الواجب pending
7. سجل الدخول كولي أمر
8. لوحة ولي الأمر ← يجب رؤية الواجب
9. أكمل الواجب: سجل ملاحظة، ارفع صوت (اختياري)
10. الحالة: completed_by_parent
11. سجل الدخول كأخصائي
12. ملف الطالب ← قسم الواجبات ← استعرض الواجب المنجز
13. قيّم (نجوم) ← الحالة: specialist_reviewed

### النتائج المتوقعة:
- الواجب يُنشأ بـ `createdFromSessionResult = 'homework'`
- لا يُنشأ واجب تلقائيًا من الجلسات
- ولي الأمر يرى فقط واجبات طفله
- الأخصائي يرى جميع واجبات طلابه

### التحقق من البيانات:
- exercises: جميع الأعمدة صحيحة
- student_specialists يحدد علاقة الطالب بالأخصائي

### Android / Windows:
✅ يختبر على كليهما
</br>

---

## Flow 9: التقارير

### الخطوات:
1. أخصائي → ملف الطالب → إنشاء تقرير
2. اختر نوع: تقرير تقدم
3. اختر جلسات للتضمين
4. اكتب ملخص (اختياري)
5. وقع إلكترونيًا
6. اعرض PDF
7. تحقق من صحة المحتوى

### النتائج المتوقعة:
- التقرير يُحفظ في قاعدة البيانات
- PDF يُنشأ ويعرض
- التقرير يظهر في قائمة التقارير في ملف الطالب

### Android / Windows:
☑️ Android يعمل. Windows قد يحتاج اختبار (الطباعة/عرض PDF).
</br>

---

## Flow 10: النسخ الاحتياطي

### الخطوات:
1. تصدير قاعدة البيانات (من شاشة الأدوات)
2. اختر مسار الحفظ
3. تحقق من وجود الملف
4. (اختياري) مسح البيانات
5. استيراد قاعدة البيانات
6. تحقق من عودة كل البيانات

### Android / Windows:
☑️ Android يعمل. Windows قد يحتاج اختبار (file_picker behavior).
</br>

---

## قائمة التحقق للاختبارات الليلية (Nightly)

```
[ ] تسجيل الدخول/الخروج بجميع الأدوار
[ ] إنشاء مركز + مدير + أخصائي + منسق
[ ] إضافة طالب + توزيعه
[ ] بناء برنامج علاجي (عادي + أصوات)
[ ] تقييم علاجي (عادي) ← حفظ ← ظهور الأهداف
[ ] تقييم علاجي (أصوات) ← حفظ ← ظهور الأهداف
[ ] جلسة: متقن ← بمساعدة ← يحتاج إعادة ← انتهاء
[ ] إعادة التدريب من ملف الطالب
[ ] واجبات: إنشاء ← عرض ← إكمال ← مراجعة
[ ] تقرير: إنشاء وعرض PDF
[ ] Dashboard الأخصائي: إحصائيات صحيحة
[ ] Dashboard المدير: إحصائيات عامة
[ ] Dashboard ولي الأمر: واجبات فقط
```

## ملاحظات الاختبار

### مشاكل معروفة تحتاج مراقبة:
1. **حساب progress**: `app.goalProgress(planId)` — هل يحسب بشكل صحيح؟ يجب التحقق من أنه يحسب من GoalSkillSteps وليس من قيمة أخرى.
2. **selectStudent بعد الحفظ**: ثقيل ويسبب وميض في الشاشة. هل يمكن تحسينه؟
3. **حفظ المسودة المتكرر**: قد يسبب بطء في التقييمات الطويلة.
4. **Android back button**: هل يتصرف بشكل صحيح في كل شاشة؟
5. **شاشة الجلسات**: هل `_applyPreselect` يعمل بشكل صحيح عند فتح إعادة التدريب؟
6. **خاص بـ Windows**: موارد الإشارة (mediaPath) — هل المسارات تعمل على Windows؟

### ما لا يختبر حاليًا:
- أداء مع 1000+ طالب
- تزامن عدة مستخدمين
- أمان (SQL injection, XSS)
- iOS
