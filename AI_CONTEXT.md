# سند — AI Context File

هذا الملف يقرأه أي AI model أو مطور قبل البدء بأي تعديل على مشروع سند.

---

## 1. Project Vision

**سند** هو نظام لإدارة التأهيل والتدخل العلاجي في مجال النطق والتخاطب. يربط النظام بين:

- **المركز** (إدارة الحالات والخطط)
- **الأخصائي** (تقييم، تدريب، جلسات)
- **الطالب** (ملف تطور، تمارين، تقارير)
- **ولي الأمر** (متابعة الواجبات والتقارير)
- **الإدارة** (إعدادات، تقارير شاملة، صلاحيات)

الهدف النهائي: نظام متكامل يدعم الأخصائي في التقييم، وضع الخطط، تنفيذ الجلسات، ومتابعة التقدم — مع الحفاظ على المرونة لمختلف أنواع التدخلات العلاجية.

---

## 2. Current Architecture

| الطبقة | التقنية |
|---|---|
| الإطار الرئيسي | Flutter (Dart) |
| إدارة الحالة | Provider (AppProvider) |
| الوصول للبيانات | Repository Pattern |
| التخزين المحلي | SQLite (sqflite) |
| المنصات المستهدفة | Android + Windows |

### Notes

- **AppProvider** ضخم حاليًا ويحتوي معظم منطق التطبيق. يجب التعامل معه بحذر.
- **Repository** يمرر جميع استعلامات SQLite.
- **Database** تُبنى داخل `_createSchema` وتُحدث عبر `_ensureLatestSchema`.
- أي تعديل في الـ Provider قد يؤثر على شاشات متعددة.

---

## 3. Current Stable Core

النواة التي نعمل على تثبيتها هي:

```
Assessment → ClinicalFindings → TrainingPlans → GoalSkillSteps → Sessions → Followups → Retrain
```

**التفاصيل:**
- التقييم ينتج نتائج (ClinicalFindings)
- النتائج تغذّي خطط التدريب (TrainingPlans)
- الخطط تحتوي أهدافًا وخطوات مهارية (GoalSkillSteps)
- الجلسات تنفذ الأهداف على الطالب
- المتابعات (Followups) تعيد التدريب عند الحاجة
- إعادة التدريب تُبنى على pending Followup فقط

---

## 4. Non-negotiable Rules

1. **لا تكسر `sourceType`** — أي تعديل في `sourceType` سيُخرب التمييز بين تقييم الأخصائي وتقييم ولي الأمر.
2. **لا تكسر `programId`** — أي تغيير في `programId` سيُفقد الربط بين العناصر والبرامج العلاجية.
3. **لا تغيّر منطق التقييم بدون طلب صريح** — التقييم معقد وحساس، أي تغيير غير مطلوب قد يسبب أخطاء متسلسلة.
4. **لا تغيّر قاعدة البيانات إلا بإضافة آمنة فقط** — ممنوع تغيير اسم جدول، حذف عمود، أو تغيير نوع عمود موجود.
5. **أي عمود جديد يُضاف في 3 أماكن إلزاميًا:**
   - `_createSchema` (لإنشاء الجداول)
   - `_upgrade` (للترقية)
   - `_ensureLatestSchema` (للإصلاح)
6. **لا تستخدم `loadHome` بعد كل `save` بدون سبب** — استخدم targeted update/reload للبيانات المتأثرة فقط لتجنب إعادة بناء كل شيء.
7. **لا تجعل "بمساعدة" أو "يحتاج إعادة" منجزًا** — حالة الإنجاز تتطلب إتقانًا حقيقيًا.
8. **`progress` لا يكون 100% إلا عند إتقان حقيقي** — النسبة تعكس مستوى الطالب الفعلي.
9. **زر إعادة التدريب يعتمد على pending `StudentFollowup` فقط** — ليس على أي حالة أخرى.
10. **لا تنشئ واجبات تلقائيًا من الجلسات** — الواجبات手动 (يدوية) حاليًا.
11. **أي تعديل يجب أن يعمل على Android وWindows** — لا تختبر على منصة واحدة فقط.

---

## 5. Platform Rules

- **Android device الأساسي:** `3c51b311` (OnePlus NE2217)
- **Windows device الأساسي:** جهاز المطور المحلي
- **Windows يجب اختباره أيضًا** بعد أي تعديل
- **لا تغيّر Gradle/Android config** إلا للضرورة القصوى
- **Hot reload:** للـ UI فقط (Widget build methods)
- **Hot restart أو rebuild:** عند تغيير state/init logic أو Provider
- **لا تمسح البيانات** إلا إذا كان السبب واضحًا ومبررًا

---

## 6. Database Rules

- **currentVersion:** 29 (حاليًا)
- **`_createSchema`:** تُبنى لأول مرة
- **`_upgrade`:** ترفع من إصدار لآخر
- **`ensureLatestSchema`:** تُصلح الجداول المفقودة أو الأعمدة الناقصة
- **لماذا حصلت مشاكل Android/Windows سابقًا:** لأن `ensureLatestSchema` لم تكن تُستدعى دائمًا بعد الترقية، مما سبب عدم تطابق بين schema المتوقع والفعلي
- **أي تعديل Schema يجب أن يكون Additive** — لا تغيير أو حذف أعمدة موجودة
- **لا hard reset للبيانات إلا للاختبار**
- **`clearAllData` للاختبار فقط** — لا تستخدمها في الإنتاج

---

## 7. Flow Rules

1. **التقييم يُولد أهدافًا نشطة** (تظهر فورًا في خطة الطالب)
2. **الهدف النشط يظهر في ملف الطالب والجلسات**
3. **`followup` ليس شرطًا لدخول الجلسة** — الجلسة تعتمد على الأهداف فقط
4. **`followup` فقط لإعادة التدريب** عند الحاجة
5. **الجلسة بعد أي تقييم تنتقل للعنصر التالي** تلقائيًا
6. **عند انتهاء العناصر كلها، تظهر رسالة انتهاء الجلسة** ولا يوجد عنصر تالي

---

## 8. UI Rules

- **Mobile-first** — التصميم يبدأ من شاشة الهاتف ثم يمتد للـ Desktop
- **Windows-friendly** — التأكد من أن التخطيط يعمل على الشاشات الكبيرة أيضًا
- **لا overflow** — أي `Row` أو `Column` يجب أن يُغلّف بمُعدّلات مرنة (`Flexible`, `Expanded`)
- **لا صفحات طويلة مزدحمة** — استخدم `ExpansionTile` / `BottomSheet` / `Dialog` عند كثرة البيانات
- **لا تغيّر المنطق أثناء redesign** — التعديل البصري فقط
- **تصميم مكتبة سند العلاجية:** هرمي (drill-down) وليس كل شيء مفتوح تحت بعض

---

## 9. Known Risk Areas

- **`AppProvider`** — ضخم، متشعب، وأي تغيير قد يؤثر على عدة شاشات
- **`selectStudent` / `loadHome`** — عمليات ثقيلة، لا تُستدعى بدون سبب
- **Assessment Wizard** — معقد وحساس، أي خطأ فيه يسبب نتائج خاطئة
- **Sessions Flow** — حساس جدًا، أي تغيير قد يسبب انقطاع تدفق الجلسات
- **`StudentProfile`** — شاشة كبيرة تحتوي تداخلات متعددة
- **Schema Migrations** — يجب عدم كسرها لأنها تؤثر على جميع المستخدمين
- **التبديل بين التبويبات في `TherapyStructureBuilderScreen`** — تسبب سابقًا خطأ `No TabController for TabBar`

---

## 10. Before Any Change Checklist

قبل البدء بأي تعديل، أجب على هذه الأسئلة:

- [ ] هل التعديل UI فقط أم logic؟
- [ ] هل يلمس database (schema أو queries)؟
- [ ] هل يلمس assessment / session / followup؟
- [ ] هل اختُبر على Android؟
- [ ] هل اختُبر على Windows؟
- [ ] هل شُغّل `flutter analyze`؟
- [ ] هل شُغّل `flutter test`؟
- [ ] هل يحتاج hot reload أم hot restart / rebuild؟
- [ ] هل يحتاج migration (إضافة عمود/جدول)؟

---

## 11. Recent Decisions (يونيو 2026)

- **لا إعادة بناء من الصفر** — نثبت الأساس الحالي ونتجنب إعادة كتابة جذرية.
- **نثبت الأساس خطوة خطوة** — كل تعديل يهدف لتثبيت جزء معين دون كسر الباقي.
- **نحل مشاكل الإدارة أولًا ثم الأخصائي** — الأولوية للشاشات الإدارية (المكتبة العلاجية، الإعدادات).
- **أي تعديل يُطبق على Android وWindows** — لا فرق بين المنصتين من ناحية الكود.
- **شاشة مكتبة سند العلاجية** — يعاد تصميمها UI فقط (drill-down hierarchy) مع الحفاظ التام على المنطق ووظائف الحفظ/الحذف.
- **إصلاح `No TabController for TabBar`** — تم باستخدام `TabController` صريح مع `SingleTickerProviderStateMixin`.
- **إصلاح overflow في `AppPill`** — تم باستخدام `Flexible` حول `Text`.
- **`_OptionPanel` overflow** — سيُحل باستخدام `BottomSheet` بدل التضمين المباشر (تم في التصميم الجديد).

---

*آخر تحديث: يونيو 2026*
